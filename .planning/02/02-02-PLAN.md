---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/components/top_nav.html
  - src/templates/base.html
autonomous: true
requirements: [FR-09]
must_haves:
  truths:
    - Botão Salvar mobile na top bar inicia hidden (sem dirty state) e expõe o seletor JS data-topbar-save-btn
    - Toda mensagem Django renderizada em base.html carrega o atributo data-django-message
    - Mensagens Django auto-removem do DOM após ~4s sem intervenção do usuário
  artifacts:
    - path: src/templates/components/top_nav.html
      provides: Botão Salvar mobile contextual com data-topbar-save-btn + estado inicial neutro/hidden
      contains: data-topbar-save-btn
    - path: src/templates/base.html
      provides: Mensagens com data-django-message e script de auto-dismiss
      contains: data-django-message
  key_links:
    - from: top_nav.html mobile button
      to: detail.html JS (Plan 03)
      via: querySelector('[data-topbar-save-btn]')
      pattern: data-topbar-save-btn
    - from: base.html messages
      to: auto-dismiss IIFE
      via: '[data-django-message] selector'
      pattern: data-django-message
---

<objective>
Preparar a "moldura" mobile: (1) ajustar o botão Salvar mobile na top bar para começar
hidden/neutro com `data-topbar-save-btn` (Plan 03 conecta o JS), e (2) tornar as mensagens
Django auto-dismiss em ~4s para servir como toast de feedback (D-19, D-22).

Purpose: Estes dois arquivos são a "chrome" da aplicação. Precisam estar prontos antes do
JS de `detail.html` (Plan 03) que conecta o `dirtyCards` Set ao novo botão e usa o
infraestrutura de messages para feedback de salvar.

Output: `top_nav.html` com botão mobile contextual (oculto até dirty); `base.html` com
mensagens auto-dismiss preservando todo o estilo atual (cores por nível).
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/02/02-CONTEXT.md
@.planning/02/02-RESEARCH.md
@.planning/02/02-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the downstream Plan 03 (detail.html JS) depends on: -->

JS selector exposed by top_nav.html:

```javascript
document.querySelector('[data-topbar-save-btn]')   // returns the mobile save <button>
```

Initial CSS state of the button (Plan 03 toggles these classes via syncTopBarButton):

- has class: `hidden bg-neutral-800 text-neutral-400`
- does NOT have: `bg-orange-500 text-black`

JS selector exposed by base.html:

```javascript
document.querySelectorAll('[data-django-message]')  // returns each rendered message <div>
```

Auto-dismiss timing contract: 4000ms before fade, 500ms fade, then `el.remove()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert mobile save button in top_nav.html to contextual hidden state</name>
  <files>src/templates/components/top_nav.html</files>
  <read_first>
    - src/templates/components/top_nav.html (full file — focus on lines 130–141 desktop button to preserve and lines 198–209 mobile button to modify)
    - .planning/02/02-PATTERNS.md (section "src/templates/components/top_nav.html" Pattern A and Pattern B — exact replacement HTML)
    - .planning/02/02-CONTEXT.md (D-02, D-04, D-20 — contextual button behaviour)
  </read_first>
  <action>
In `src/templates/components/top_nav.html`:

1. **Preserve verbatim** the desktop save button block (the `{% if current_view == 'pool:detail' and active_tab == 'bets' %}` block currently around lines 130–141 that produces the `bg-orange-500` "Salvar palpites" desktop button). Do NOT touch this block.

1. **Replace** the mobile save button block (currently around lines 198–209 — a button with classes `... bg-orange-500 ...` rendered inside the mobile top bar). The new markup is exactly:

```html
{% if current_view == 'pool:detail' and active_tab == 'bets' %}
<button
    type="submit"
    form="pool-bets-form"
    name="submit_action"
    value="save_all"
    data-topbar-save-btn
    class="hidden absolute left-1/2 -translate-x-1/2 px-3 py-1.5 rounded-md text-sm font-semibold disabled:opacity-50 disabled:cursor-not-allowed bg-neutral-800 text-neutral-400"
    {% if not can_bet or group_locked and knockout_locked %}disabled{% endif %}
>
    Salvar
</button>
{% endif %}
```

Key concrete differences from the previous mobile button:

- Adds attribute `data-topbar-save-btn` (Plan 03 JS selector target)
- Adds class `hidden` (JS removes when dirty per D-02/D-20)
- REMOVES classes `bg-orange-500` and `text-black`
- ADDS classes `bg-neutral-800` and `text-neutral-400` (neutral resting state)
- Keeps `absolute left-1/2 -translate-x-1/2` (centered in mobile top bar — D-02)
- Keeps `disabled` template logic verbatim (`{% if not can_bet or group_locked and knockout_locked %}disabled{% endif %}`)
- Label changes from "Salvar palpites" to "Salvar" (shorter for mobile centered placement; desktop button keeps "Salvar palpites")

3. Do NOT modify the hamburger toggle, the back arrow, the desktop nav links, or any other element in the file.
   </action>
   <verify>
   <automated>poetry run python -c "s=open('src/templates/components/top_nav.html',encoding='utf-8').read(); assert 'data-topbar-save-btn' in s; assert 'hidden absolute left-1/2 -translate-x-1/2' in s; assert 'bg-neutral-800 text-neutral-400' in s; assert s.count('bg-orange-500') >= 1, 'desktop button must still use bg-orange-500'; print('OK')"</automated>
   </verify>
   \<acceptance_criteria>
   - `grep -c "data-topbar-save-btn" src/templates/components/top_nav.html` returns 1
   - `grep -c "hidden absolute left-1/2 -translate-x-1/2" src/templates/components/top_nav.html` returns 1
   - `grep -c "bg-neutral-800 text-neutral-400" src/templates/components/top_nav.html` returns >= 1
   - `grep -c "bg-orange-500 text-black font-semibold" src/templates/components/top_nav.html` returns >= 1 (desktop button preserved)
   - `grep -c "form=\"pool-bets-form\"" src/templates/components/top_nav.html` returns 2 (desktop + mobile)
   - `grep -c "name=\"submit_action\"" src/templates/components/top_nav.html` returns 2
   - `poetry run python -m src.manage check` exits 0
   - `poetry run python -m src.manage validate_templates 2>/dev/null || poetry run python -c "from django.template.loader import get_template; from django.conf import settings; import django; import os; os.environ.setdefault('DJANGO_SETTINGS_PROFILE','test'); django.setup(); get_template('components/top_nav.html')"` does not raise
     \</acceptance_criteria>
     <done>
     Mobile save button is hidden by default, neutral colored, exposes `data-topbar-save-btn`, while the desktop button retains the previous orange visible behavior.
     </done>
     </task>

<task type="auto">
  <name>Task 2: Add data-django-message attribute and auto-dismiss script to base.html</name>
  <files>src/templates/base.html</files>
  <read_first>
    - src/templates/base.html (full file — 46 lines; focus on lines 23–33 messages block and lines 39–41 around extra_scripts and lucide.createIcons)
    - .planning/02/02-PATTERNS.md (section "src/templates/base.html" Pattern A and Pattern B — exact attribute and script placement)
    - .planning/02/02-RESEARCH.md (Pitfall 5 about z-index conflicts — keep messages in <main>, do not reposition as fixed)
  </read_first>
  <action>
In `src/templates/base.html`:

1. **In the messages loop** (currently around lines 25–32), the inner `<div>` that renders each message currently looks like:

```html
<div
    class="mb-2 rounded-md border px-4 py-3 text-sm {% if 'success' in message.tags %}border-green-500/30 bg-green-500/10 text-green-300{% elif 'warning' in message.tags %}border-amber-500/30 bg-amber-500/10 text-amber-300{% elif 'error' in message.tags %}border-red-500/30 bg-red-500/10 text-red-300{% else %}border-blue-500/30 bg-blue-500/10 text-blue-300{% endif %}"
    role="alert"
>
    {{ message }}
</div>
```

Add the attribute `data-django-message` directly after the opening `<div` (and BEFORE the `class=`). The full conditional class string MUST be preserved verbatim — do NOT touch the success/warning/error/info color logic. Resulting tag:

```html
<div
    data-django-message
    class="mb-2 rounded-md border px-4 py-3 text-sm {% if 'success' in message.tags %}border-green-500/30 bg-green-500/10 text-green-300{% elif 'warning' in message.tags %}border-amber-500/30 bg-amber-500/10 text-amber-300{% elif 'error' in message.tags %}border-red-500/30 bg-red-500/10 text-red-300{% else %}border-blue-500/30 bg-blue-500/10 text-blue-300{% endif %}"
    role="alert"
>
    {{ message }}
</div>
```

2. **Insert the auto-dismiss script** after the existing `{% block extra_scripts %}{% endblock %}` (line 39) and BEFORE the existing `<script>` containing `lucide.createIcons()` (line 41). Insert exactly:

```html
<script>
(function () {
    document.querySelectorAll('[data-django-message]').forEach(function (el) {
        setTimeout(function () {
            el.style.transition = 'opacity 0.5s ease';
            el.style.opacity = '0';
            setTimeout(function () { el.remove(); }, 500);
        }, 4000);
    });
})();
</script>
```

3. Do NOT relocate the messages container, do NOT add `position: fixed` or any z-index manipulation (per Pitfall 5 — messages stay inside `<main>` with the existing padding-top). Do NOT remove or alter the existing `lucide.createIcons()` call. Do NOT alter the `{% block extra_scripts %}` placement.
   </action>
   <verify>
   <automated>poetry run python -c "s=open('src/templates/base.html',encoding='utf-8').read(); assert 'data-django-message' in s; assert s.count('data-django-message') >= 2, 'attribute on div + selector in script'; assert "document.querySelectorAll('[data-django-message]')" in s; assert 'lucide.createIcons()' in s; assert 'border-green-500/30 bg-green-500/10 text-green-300' in s, 'color classes preserved'; print('OK')"</automated>
   </verify>
   \<acceptance_criteria>
   - `grep -c "data-django-message" src/templates/base.html` returns >= 2 (HTML attribute + JS selector)
   - `grep -c "querySelectorAll('\\[data-django-message\\]')" src/templates/base.html` returns 1
   - `grep -c "setTimeout(function () { el.remove(); }, 500)" src/templates/base.html` returns 1
   - `grep -c "}, 4000);" src/templates/base.html` returns 1
   - `grep -c "lucide.createIcons()" src/templates/base.html` returns 1 (preserved)
   - `grep -c "border-green-500/30 bg-green-500/10 text-green-300" src/templates/base.html` returns 1 (success color preserved)
   - `grep -c "border-red-500/30 bg-red-500/10 text-red-300" src/templates/base.html` returns 1 (error color preserved)
   - `grep -c "{% block extra_scripts %}" src/templates/base.html` returns 1 (block preserved)
   - `poetry run python -m src.manage check` exits 0
   - Template loads without TemplateSyntaxError: `poetry run python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_PROFILE','test'); import django; django.setup(); from django.template.loader import get_template; get_template('base.html')"` exits 0
     \</acceptance_criteria>
     <done>
     Each Django message div has `data-django-message`, an auto-dismiss IIFE removes them after 4s + 500ms fade, and all existing styling/scripts are preserved.
     </done>
     </task>

</tasks>

<verification>
- `poetry run python -m src.manage check` passes
- `DJANGO_SETTINGS_PROFILE=test poetry run python -m src.manage test --verbosity=1` runs the full suite without regressions (no view/template was renamed)
- Manual visual check deferred to Plan 03 (where the integrated JS is wired); this plan is purely a chrome change.
</verification>

\<success_criteria>

- Mobile save button starts hidden + neutral and exposes the JS hook (`data-topbar-save-btn`)
- Desktop save button untouched
- All Django messages auto-dismiss after ~4s; styling preserved per level
- No regression in template loading or `manage check`
  \</success_criteria>

<output>
After completion, create `.planning/02/02-02-SUMMARY.md` documenting:
- The final HTML of the mobile save button (so Plan 03 has the exact selector + initial classes)
- The final auto-dismiss script block (so Plan 03 knows the timing contract)
- Confirmation that desktop button + lucide block were not modified
</output>
