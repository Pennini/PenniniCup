---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/pool/templates/pool/detail.html
autonomous: false
requirements: [FR-01, FR-03, FR-09]
user_setup: []
must_haves:
  truths:
    - Cards de palpites grupo e mata-mata renderizam em layout de linha única `Time A [00]:[00] Time B` legível em 320px
    - Inputs de placar têm 44px (`w-11 h-11`) para toque confortável
    - Acima da lista de cards aparece barra de progresso `saved_bets_count/total_group_matches` + toggle Por Data | Por Grupo (somente quando active_tab='bets')
    - Cards de fase de grupos exibem cabeçalhos dinâmicos por data (padrão D-08) ou por grupo (toggle), inseridos via JS sem duplicar inputs
    - Cards de mata-mata sempre agrupam por fase (Oitavas/Quartas/Semi/Final) — toggle Por Data/Por Grupo não se aplica a eles (D-12)
    - Botão Salvar da top bar mobile fica hidden quando não há dirty state e laranja+visível quando dirtyCards.size > 0
    - Inputs disabled e badge 'Fechado' continuam funcionando para fases bloqueadas (D-21)
    - beforeunload warning preservado para campos sujos não salvos (D-03)
    - Toggle de agrupamento NÃO persiste entre sessões — cada visita reseta para 'Por Data' (D-11)
  artifacts:
    - path: src/pool/templates/pool/detail.html
      provides: 'Mobile-first bet UI: cards compactos, progress bar, grouping toggle, JS sync'
      contains: data-topbar-save-btn
  key_links:
    - from: detail.html JS
      to: top_nav.html mobile save button (Plan 02)
      via: querySelector('[data-topbar-save-btn]')
      pattern: data-topbar-save-btn
    - from: detail.html progress bar
      to: context_builder.py (Plan 01)
      via: '{{ saved_bets_count }} / {{ total_group_matches }}'
      pattern: saved_bets_count.*total_group_matches
    - from: detail.html grouping JS
      to: card data-attributes
      via: data-match-card[data-phase=GROUP] data-match-date data-match-group
      pattern: data-match-card.*data-match-date.*data-match-group
---

<objective>
Refatorar `src/pool/templates/pool/detail.html` para mobile-first: cards compactos em
linha única, barra de progresso + toggle "Por Data | Por Grupo" (D-05–D-08), agrupamento
JS sem duplicar inputs (Pitfall 4), cards de mata-mata com cabeçalho por fase (D-12), e
JS que sincroniza o botão Salvar da top bar com o `dirtyCards` Set existente (D-02/D-20).

Purpose: Esta é a peça central da Phase 2. Tudo que veio antes (Plan 01 fornece
counters/ordenação; Plan 02 fornece o `data-topbar-save-btn` e auto-dismiss de toast)
existe para que este refactor seja "drop-in".

Output: `detail.html` reescrito mantendo toda a infraestrutura JS existente
(`dirtyCards`, `beforeunload`, `[data-winner-select]`, AJAX `save_bet`) e estendendo-a
com agrupamento, sync de top bar, e novo layout de card.
</objective>

\<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
\</execution_context>

<context>
@.planning/02/02-CONTEXT.md
@.planning/02/02-RESEARCH.md
@.planning/02/02-PATTERNS.md
@.planning/02/02-01-SUMMARY.md
@.planning/02/02-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts from upstream plans this plan consumes: -->

From Plan 01 (`context_builder.py`) — the view context now contains:

```python
"saved_bets_count": int   # number of group_rows where bet.is_active is True
"total_group_matches": int  # len(group_rows)
# group_rows is now sorted by match.match_date_brasilia ascending
```

From Plan 02 (`top_nav.html`) — JS selector and contract:

```javascript
const mobileTopBarBtn = document.querySelector('[data-topbar-save-btn]');
// initial state: classes "hidden bg-neutral-800 text-neutral-400"
// when dirty:    remove "hidden bg-neutral-800 text-neutral-400", add "bg-orange-500 text-black"
```

From Plan 02 (`base.html`) — every Django message is auto-dismissed after 4s; emit messages from `save_bets_bulk` (already done in views.py) and they appear as toasts automatically. No JS work required here for toasts.

Existing JS infrastructure in `detail.html` to PRESERVE:

- `dirtyCards` Set tracking
- `initialPayloadByCard` Map + `currentPayloadKey()`
- `refreshDirtyState(card)` function
- `hasPendingChanges()`
- `beforeunload` listener
- `[data-winner-select]` knockout select logic
- AJAX `save_bet` per-match endpoint usage (if any)
- Tab toggle include `pool_bet_toggle_three_tabs.html`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor detail.html cards and add progress + grouping toggle markup</name>
  <files>src/pool/templates/pool/detail.html</files>
  <read_first>
    - src/pool/templates/pool/detail.html (FULL file — 497 lines; you must understand the existing card markup, the form wrapper, and the JS IIFE before editing)
    - .planning/02/02-PATTERNS.md (sections "src/pool/templates/pool/detail.html" Patterns A through F — these are concrete copy-paste snippets with exact line references and class strings)
    - .planning/02/02-RESEARCH.md (Pattern 5 mobile card layout, Pattern 1 grouping pre-render, Pitfall 4 about input duplication — adopt the JS-grouping approach to avoid duplicate inputs)
    - .planning/02/02-CONTEXT.md (D-13 through D-22 for visual feedback rules)
    - src/templates/components/top_nav.html (already updated in Plan 02 — confirm `data-topbar-save-btn` exists)
  </read_first>
  <action>
Edit `src/pool/templates/pool/detail.html`. Apply ALL of the following changes in one edit pass. Preserve the rest of the file (artilheiro card, form wrapper, CSRF token, includes) verbatim.

**A. Add data-attributes to every group card opening `<article>` tag** (current group card around lines 69–70). The current opening already has:

```html
<article class="rounded-xl border border-neutral-700 p-4 bg-neutral-900"
         data-match-card
         data-phase="{{ row.phase }}"
         data-save-url="{% url 'pool:save-bet' pool.slug row.match.id %}">
```

Append these three new data-attributes (keep all existing attributes):

```html
         data-match-date="{{ row.match.match_date_brasilia|date:'Y-m-d' }}"
         data-match-date-label="{{ row.match.match_date_brasilia|date:'d/m - l' }}"
         data-match-group="{{ row.match.group.name|default:'' }}"
```

Apply the SAME `data-match-date` and `data-match-date-label` additions to every knockout card `<article>` (around lines 232–236), but for knockout use `data-stage` instead of `data-match-group`:

```html
         data-match-date="{{ row.match.match_date_brasilia|date:'Y-m-d' }}"
         data-match-date-label="{{ row.match.match_date_brasilia|date:'d/m - l' }}"
         data-stage="{{ row.match.stage.name|default:'' }}"
```

**B. Replace the card header block** (current `<div>` block around lines 70–115 that contains the phase/group label, match number, and the Aberto/Fechado pill) with the unified flex header per Pattern C:

```html
<div class="flex items-center justify-between gap-2 mb-3">
  <div class="flex items-center gap-2 min-w-0">
    <p class="text-xs uppercase tracking-wide text-neutral-400 truncate">
      {% if row.phase == "GROUP" %}Grupo {{ row.match.group.name|default:"-" }}
      {% else %}{{ row.match.stage.name|default:"Mata-mata" }}{% endif %}
    </p>
    <span class="text-xs text-neutral-600">#{{ row.match.match_number }}</span>
  </div>
  {% if row.locked %}
  <span class="text-xs px-2 py-1 rounded-full border border-red-400 text-red-300 shrink-0">Fechado</span>
  {% else %}
  <span class="text-xs px-2 py-1 rounded-full border border-emerald-400 text-emerald-300 shrink-0">Aberto</span>
  {% endif %}
</div>
```

**C. Replace the score-input grid block** (current 2-column grid around lines 119–181 that has `<div class="grid gap-3 min-[900px]:grid-cols-2">` containing two sub-cards — one for home, one for away) with the single-line mobile layout per Pattern B from PATTERNS.md. CRITICAL: the `name`, `value`, `data-score-home`/`data-score-away`, `placeholder`, `aria-label`, and `disabled` logic of the inputs MUST be copied verbatim from the existing inputs (current lines ~138–148 and ~169–179) so the existing JS (`dirtyCards`, AJAX) keeps working. The replacement is:

```html
<div class="mt-3 flex items-center gap-2 w-full">
  <div class="flex items-center gap-1.5 flex-1 justify-end min-w-0">
    {% if row.home_team and row.home_team.flag_image_url %}
    <span class="inline-flex h-5 w-5 overflow-hidden rounded-full ring-1 ring-neutral-600 bg-neutral-800 shrink-0">
      <img src="{{ row.home_team.flag_image_url }}" alt="Bandeira {{ row.home_team.name }}"
           class="h-full w-full object-cover" loading="lazy" />
    </span>
    {% endif %}
    <span class="truncate text-sm font-medium text-right">
      {{ row.home_team.name|default:row.match.home_placeholder|default:"A definir" }}
    </span>
  </div>

  <div class="flex items-center gap-1 shrink-0">
    <input type="number" min="0"
           name="match_{{ row.match.id }}_home_score_pred"
           value="{% if row.bet %}{{ row.bet.home_score_pred }}{% endif %}"
           data-score-home
           class="w-11 h-11 rounded-md bg-black border border-neutral-700 text-center text-lg font-bold"
           placeholder="0"
           aria-label="Palpite de gols do time da casa"
           {% if row.locked or not can_bet %}disabled{% endif %} />
    <span class="text-neutral-500 font-bold px-0.5">:</span>
    <input type="number" min="0"
           name="match_{{ row.match.id }}_away_score_pred"
           value="{% if row.bet %}{{ row.bet.away_score_pred }}{% endif %}"
           data-score-away
           class="w-11 h-11 rounded-md bg-black border border-neutral-700 text-center text-lg font-bold"
           placeholder="0"
           aria-label="Palpite de gols do time visitante"
           {% if row.locked or not can_bet %}disabled{% endif %} />
  </div>

  <div class="flex items-center gap-1.5 flex-1 justify-start min-w-0">
    <span class="truncate text-sm font-medium">
      {{ row.away_team.name|default:row.match.away_placeholder|default:"A definir" }}
    </span>
    {% if row.away_team and row.away_team.flag_image_url %}
    <span class="inline-flex h-5 w-5 overflow-hidden rounded-full ring-1 ring-neutral-600 bg-neutral-800 shrink-0">
      <img src="{{ row.away_team.flag_image_url }}" alt="Bandeira {{ row.away_team.name }}"
           class="h-full w-full object-cover" loading="lazy" />
    </span>
    {% endif %}
  </div>
</div>
```

Apply the SAME single-line replacement to the knockout card score block (currently around lines 282–342). Use the exact same input markup. The knockout-specific `[data-winner-wrapper]` + `[data-winner-select]` block (currently around lines 184–201 for groups and 347–365 for knockouts — the latter is the relevant one for tiebreaker classifier per D-15) MUST be preserved verbatim AFTER the new score line in the knockout card. Do NOT rename `data-winner-wrapper`, `data-winner-select`, or `data-base-disabled`.

**D. Insert progress bar + grouping toggle** between the existing `{% include "components/pool_bet_toggle_three_tabs.html" %}` line (currently line 26) and the `{% if active_tab == "classification" %}` check (line 28). Add this exact block (only renders for the bets tab; mata-mata uses the same toggle but JS will hide the buttons when applicable per D-12):

```html
{% if active_tab == "bets" %}
<div class="flex items-center gap-3" id="phase2-progress-row">
  <div class="flex-1 h-1.5 rounded-full bg-neutral-800">
    <div
      class="h-1.5 rounded-full bg-orange-500 transition-all"
      style="width: {% widthratio saved_bets_count total_group_matches 100 %}%"
    ></div>
  </div>
  <span class="text-xs text-neutral-400 whitespace-nowrap">
    {{ saved_bets_count }}/{{ total_group_matches }}
  </span>

  <div class="flex rounded-lg border border-neutral-700 overflow-hidden shrink-0" id="grouping-toggle">
    <button type="button" id="toggle-by-date"
            class="px-2.5 py-1 text-xs font-semibold bg-neutral-800 text-orange-200 transition-colors"
            aria-pressed="true">Por Data</button>
    <button type="button" id="toggle-by-group"
            class="px-2.5 py-1 text-xs font-semibold text-neutral-400 hover:text-neutral-200 transition-colors"
            aria-pressed="false">Por Grupo</button>
  </div>
</div>
{% endif %}
```

CRITICAL: the toggle buttons MUST be `type="button"` so they do not submit the form.

**E. Knockout phase grouping headers** (D-12: mata-mata sempre por fase, toggle não se aplica). Per Pitfall 4 we keep one render. The existing knockout loop already iterates per row — wrap it so JS later inserts headers using `data-stage`. No structural change needed beyond the data-attribute added in step A; the JS in Task 2 will inject `data-grouping-header` divs labeled `{stage.name}` for knockouts. Verify the knockout loop is wrapped in a container with id `knockout-list`. If no such id exists, add `id="knockout-list"` to the wrapping `<div>` of the knockout cards.

For the group-phase loop wrapper, ensure (or add) `id="group-list"` on the wrapping container that immediately surrounds the `{% for row in group_rows %}` loop. The JS in Task 2 targets `#group-list` to know which cards to regroup.

Do NOT add any other structural change to the file. Do NOT remove existing classes used by the JS (`data-bet-card-cell`, `data-score-home`, `data-score-away`, `data-winner-wrapper`, `data-winner-select`, etc.).
</action>
<verify>
<automated>poetry run python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_PROFILE','test'); import django; django.setup(); from django.template.loader import get_template; t=get_template('pool/detail.html'); s=open('src/pool/templates/pool/detail.html',encoding='utf-8').read(); assert 'data-match-date=' in s; assert 'data-match-date-label=' in s; assert 'data-match-group=' in s; assert 'data-stage=' in s; assert 'id="grouping-toggle"' in s; assert 'id="toggle-by-date"' in s; assert 'id="toggle-by-group"' in s; assert 'widthratio saved_bets_count total_group_matches 100' in s; assert s.count('data-winner-select') >= 1, 'knockout select preserved'; assert s.count('data-score-home') >= 1; assert s.count('data-score-away') >= 1; assert 'id="group-list"' in s; assert 'id="knockout-list"' in s; print('OK')"</automated>
</verify>
\<acceptance_criteria>
\- Template loads without TemplateSyntaxError (verify command succeeds)
\- `grep -c "data-match-date=" src/pool/templates/pool/detail.html` returns >= 2 (group + knockout)
\- `grep -c "data-match-date-label=" src/pool/templates/pool/detail.html` returns >= 2
\- `grep -c "data-match-group=" src/pool/templates/pool/detail.html` returns >= 1
\- `grep -c "data-stage=" src/pool/templates/pool/detail.html` returns >= 1
\- `grep -c "id=\"grouping-toggle\"" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "id=\"toggle-by-date\"" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "id=\"toggle-by-group\"" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "id=\"group-list\"" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "id=\"knockout-list\"" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "widthratio saved_bets_count total_group_matches 100" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "w-11 h-11" src/pool/templates/pool/detail.html` returns >= 4 (2 group inputs + 2 knockout inputs minimum)
\- `grep -c "data-winner-select" src/pool/templates/pool/detail.html` returns >= 1 (preserved)
\- `grep -c "data-winner-wrapper" src/pool/templates/pool/detail.html` returns >= 1 (preserved)
\- `grep -cE 'border border-(red|emerald)-400' src/pool/templates/pool/detail.html` returns >= 2 (Aberto/Fechado pills preserved)
\- `grep -c "type=\"button\" id=\"toggle-by-date\"" src/pool/templates/pool/detail.html` returns 1 (button MUST NOT submit form)
\- `poetry run python -m src.manage check` exits 0
\</acceptance_criteria>
<done>
Template renders without errors, all data-attributes / progress markup / toggle buttons present, existing JS hooks preserved.
</done>
</task>

<task type="auto">
  <name>Task 2: Extend detail.html JS IIFE with top-bar sync and grouping logic</name>
  <files>src/pool/templates/pool/detail.html</files>
  <read_first>
    - src/pool/templates/pool/detail.html (re-read AFTER Task 1 so you see the current `<script>` IIFE which spans roughly lines 371–494; locate `dirtyCards`, `refreshDirtyState`, and the IIFE closing `})();`)
    - .planning/02/02-PATTERNS.md (Pattern F — JS insertion points and exact snippets)
    - .planning/02/02-RESEARCH.md (Code Examples section — top bar sync + grouping function)
    - .planning/02/02-CONTEXT.md (D-11 toggle does NOT persist; D-12 mata-mata sempre por fase)
  </read_first>
  <action>
Edit the existing `<script>` IIFE inside `src/pool/templates/pool/detail.html` (the IIFE that begins with `(function () { const form = ...` and ends with `})();`). Make these additions; do NOT delete any existing JS:

**A. Insert top-bar sync block** immediately AFTER the existing `let isSubmitting = false;` line (or, if not present, after the `dirtyCards` Set declaration):

```javascript
const mobileTopBarBtn = document.querySelector('[data-topbar-save-btn]');
function syncTopBarButton() {
    if (!mobileTopBarBtn) return;
    if (mobileTopBarBtn.disabled) {
        mobileTopBarBtn.classList.add('hidden');
        return;
    }
    const pending = hasPendingChanges();
    mobileTopBarBtn.classList.toggle('hidden', !pending);
    mobileTopBarBtn.classList.toggle('bg-orange-500', pending);
    mobileTopBarBtn.classList.toggle('text-black', pending);
    mobileTopBarBtn.classList.toggle('bg-neutral-800', !pending);
    mobileTopBarBtn.classList.toggle('text-neutral-400', !pending);
}
```

NOTE: `hasPendingChanges` is defined later in the IIFE — JavaScript function declarations hoist, but `mobileTopBarBtn` is `const` so this block must appear AFTER the IIFE prologue (not at the very top) to avoid TDZ issues if `hasPendingChanges` ends up as `const`. Inspect first; if `hasPendingChanges` is a `function` declaration it hoists and order is irrelevant.

**B. Add the `syncTopBarButton()` call at the end of the existing `refreshDirtyState(card)` function**. The function currently looks like (around lines 405–414):

```javascript
function refreshDirtyState(card) {
    const initialPayload = initialPayloadByCard.get(card);
    const currentPayload = currentPayloadKey(card);
    if (initialPayload === currentPayload) {
        dirtyCards.delete(card);
    } else {
        dirtyCards.add(card);
    }
    syncTopBarButton();   // <-- ADD THIS LINE as the last line of the function body
}
```

**C. Insert the grouping function** AFTER `syncTopBarButton` definition:

```javascript
function applyGrouping(mode) {
    document.querySelectorAll('[data-grouping-header]').forEach(function (el) { el.remove(); });

    const groupCards = document.querySelectorAll('#group-list [data-match-card][data-phase="GROUP"]');
    let lastKey = null;
    groupCards.forEach(function (card) {
        const key   = mode === 'date' ? card.dataset.matchDate      : card.dataset.matchGroup;
        const label = mode === 'date' ? card.dataset.matchDateLabel : 'Grupo ' + card.dataset.matchGroup;
        if (key !== lastKey) {
            const header = document.createElement('h3');
            header.setAttribute('data-grouping-header', '');
            header.className = 'text-xs uppercase tracking-wide text-neutral-400 mt-4 mb-2 px-1';
            header.textContent = label;
            card.parentNode.insertBefore(header, card);
            lastKey = key;
        }
    });

    const btnDate  = document.getElementById('toggle-by-date');
    const btnGroup = document.getElementById('toggle-by-group');
    if (btnDate && btnGroup) {
        btnDate.classList.toggle('bg-neutral-800',   mode === 'date');
        btnDate.classList.toggle('text-orange-200',  mode === 'date');
        btnDate.classList.toggle('text-neutral-400', mode !== 'date');
        btnGroup.classList.toggle('bg-neutral-800',   mode === 'group');
        btnGroup.classList.toggle('text-orange-200',  mode === 'group');
        btnGroup.classList.toggle('text-neutral-400', mode !== 'group');
        btnDate.setAttribute('aria-pressed',  mode === 'date'  ? 'true' : 'false');
        btnGroup.setAttribute('aria-pressed', mode === 'group' ? 'true' : 'false');
    }
}

// Knockout always groups by stage (D-12) — independent of the Por Data / Por Grupo toggle.
function applyKnockoutStageGrouping() {
    const stageCards = document.querySelectorAll('#knockout-list [data-match-card][data-stage]');
    let lastStage = null;
    stageCards.forEach(function (card) {
        const stage = card.dataset.stage;
        if (stage && stage !== lastStage) {
            const header = document.createElement('h3');
            header.setAttribute('data-grouping-header', '');
            header.className = 'text-xs uppercase tracking-wide text-neutral-400 mt-4 mb-2 px-1';
            header.textContent = stage;
            card.parentNode.insertBefore(header, card);
            lastStage = stage;
        }
    });
}
```

**D. Wire up the toggle buttons + initialize** by inserting the following BEFORE the IIFE's closing `})();`:

```javascript
const btnToggleDate  = document.getElementById('toggle-by-date');
const btnToggleGroup = document.getElementById('toggle-by-group');
if (btnToggleDate)  btnToggleDate.addEventListener('click',  function () { applyGrouping('date'); });
if (btnToggleGroup) btnToggleGroup.addEventListener('click', function () { applyGrouping('group'); });
// D-08 default: by date. D-11: do NOT read from localStorage / cookie.
applyGrouping('date');
applyKnockoutStageGrouping();
syncTopBarButton();
```

CRITICAL — D-11 enforcement: do NOT read or write `localStorage`, `sessionStorage`, or any cookie for the toggle state. The default is always `date` on every visit.

**E. Do NOT touch** the `beforeunload` listener (D-03), the `[data-winner-select]` handlers (D-15), the AJAX `save_bet` calls, or the existing form submit handler.
</action>
<verify>
<automated>poetry run python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_PROFILE','test'); import django; django.setup(); from django.template.loader import get_template; t=get_template('pool/detail.html'); s=open('src/pool/templates/pool/detail.html',encoding='utf-8').read(); assert 'syncTopBarButton' in s; assert s.count('syncTopBarButton') >= 4, 'definition + call inside refreshDirtyState + init + at least one toggle path'; assert 'applyGrouping' in s; assert s.count('applyGrouping') >= 4; assert 'applyKnockoutStageGrouping' in s; assert 'data-topbar-save-btn' in s; assert 'beforeunload' in s, 'beforeunload preserved'; assert 'localStorage' not in s, 'D-11 forbids persistence'; assert 'sessionStorage' not in s, 'D-11 forbids persistence'; print('OK')"</automated>
</verify>
\<acceptance_criteria>
\- `grep -c "syncTopBarButton" src/pool/templates/pool/detail.html` returns >= 4 (definition + refreshDirtyState call + init call + at least one mode-toggle internal call)
\- `grep -c "applyGrouping" src/pool/templates/pool/detail.html` returns >= 4 (definition + 2 button listeners + init call)
\- `grep -c "applyKnockoutStageGrouping" src/pool/templates/pool/detail.html` returns >= 2 (definition + init call)
\- `grep -c "querySelector('\\[data-topbar-save-btn\\]')" src/pool/templates/pool/detail.html` returns 1
\- `grep -c "beforeunload" src/pool/templates/pool/detail.html` returns >= 1 (preserved)
\- `grep -c "localStorage" src/pool/templates/pool/detail.html` returns 0 (D-11)
\- `grep -c "sessionStorage" src/pool/templates/pool/detail.html` returns 0 (D-11)
\- `grep -c "data-winner-select" src/pool/templates/pool/detail.html` returns >= 1 (preserved)
\- `grep -c "data-grouping-header" src/pool/templates/pool/detail.html` returns >= 3 (cleanup query + 2 createElement attribute set)
\- Template parses: `poetry run python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_PROFILE','test'); import django; django.setup(); from django.template.loader import get_template; get_template('pool/detail.html')"` exits 0
\- `poetry run python -m src.manage check` exits 0
\- Full test suite runs (no view regression): `DJANGO_SETTINGS_PROFILE=test poetry run python -m src.manage test src.pool --verbosity=1` exits 0
\</acceptance_criteria>
<done>
JS IIFE extended with top-bar sync, group-phase regrouping, knockout stage headers, and init wiring; D-11 enforced (no persistence); existing handlers untouched.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of mobile palpites flow</name>
  <what-built>
Plan 02-03 reformulou `detail.html` para mobile-first com cards compactos, barra de progresso, toggle "Por Data | Por Grupo", botão Salvar contextual na top bar e toast auto-dismiss. Plan 01 adicionou os counters; Plan 02 ajustou top_nav.html e base.html. Esta é a verificação visual integrada.
  </what-built>
  <how-to-verify>
1. Suba o servidor: `make runserver` (ou `poetry run python -m src.manage runserver`).
2. Abra o devtools do browser e ative responsive mode em **375x667** (iPhone SE) e depois **320x568** (mais estreito permitido por D-13).
3. Navegue até a página de detalhe de um Pool autenticado como participante apto a palpitar (`active_tab=bets`).
4. Verifique cada item:
   - **Cards de grupo:** layout em linha única `Time A [00] : [00] Time B`. Inputs claramente clicáveis (44x44px). Nomes longos truncam sem quebrar layout em 320px.
   - **Cabeçalho do card:** `Grupo X #N` à esquerda, badge `Aberto`/`Fechado` à direita, mesma linha.
   - **Progress bar + toggle:** abaixo do toggle de fase (Grupos/Mata-mata/Classificação). Mostra `N/M` e proporção em laranja. Botões `Por Data` (ativo, fundo neutro + texto laranja-200) | `Por Grupo` (inativo, texto neutro).
   - **Default Por Data:** ao abrir a aba, cabeçalhos `dd/mm - DiaDaSemana` aparecem inseridos entre os cards.
   - **Toggle para Por Grupo:** clicar troca cabeçalhos para `Grupo A`, `Grupo B`, etc.
   - **Recarregar página (F5):** volta para `Por Data` mesmo após ter clicado em Por Grupo (D-11).
   - **Mata-mata:** mudar para a aba mata-mata; cabeçalhos por fase (Oitavas/Quartas/Semi/Final) sempre presentes; toggle Por Data/Por Grupo NÃO afeta o mata-mata (D-12).
   - **Card mata-mata com palpite empate:** select "Classificado: [Time ▾]" continua aparecendo abaixo do placar.
   - **Top bar mobile (< lg breakpoint):** botão central `Salvar` invisível inicialmente. Digitar um placar → botão aparece em laranja. Reverter o placar → botão volta a ficar oculto.
   - **Submit pelo botão da top bar:** clica "Salvar" → POST → redirect → toast de sucesso ("Palpites salvos!" ou similar) aparece no topo da página e some sozinho em ~4s.
   - **Cenário de fase bloqueada:** com uma `Pool.is_phase_locked(GROUP)=True`, verificar inputs `disabled`, badge `Fechado` visível em cada card, botão Salvar permanece disabled e oculto.
   - **`beforeunload` warning:** alterar um placar e tentar fechar a aba → browser pede confirmação.

5. Verifique também desktop (≥ lg breakpoint): botão "Salvar palpites" laranja na top bar continua igual (não foi modificado). Layout dos cards continua usável (mesmo com flex layout — não quebra em telas largas).

1. Se TODOS os itens acima passarem, escreva `approved`. Caso contrário, descreva exatamente qual item falhou e em qual viewport/cenário.
   </how-to-verify>
   <resume-signal>Type "approved" or describe issues</resume-signal>
   </task>

</tasks>

\<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                               |
| ----------------------------- | ------------------------------------------------------------------------- |
| Browser → save_bets_bulk POST | Form submitted via existing `pool-bets-form`; no new endpoint introduced. |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                               |
| ---------------- | ---------------------- | --------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01          | Tampering              | Mobile save button (form attribute)           | accept      | Reuses `form="pool-bets-form"` to existing endpoint with `@login_required`, `@ratelimit`, `ATOMIC_REQUESTS`, CSRF token already in form. No new attack surface.               |
| T-02-02          | Information disclosure | Card data-attributes (match_date, group name) | accept      | Same data already exposed in card text/labels; data-attributes only mirror what the user already sees.                                                                        |
| T-02-03          | Denial of service      | Toggle buttons rapidly clicked                | mitigate    | `applyGrouping` is idempotent: removes previous headers before inserting new ones; cost is O(n) on group_rows length (typically ~48 matches in a Copa). No throttling needed. |
| \</threat_model> |                        |                                               |             |                                                                                                                                                                               |

<verification>
- All Task 1 + Task 2 grep gates pass
- `poetry run python -m src.manage check` exits 0
- `DJANGO_SETTINGS_PROFILE=test poetry run python -m src.manage test src.pool --verbosity=1` exits 0 (no test regression)
- Human checkpoint passes (`approved`)
</verification>

\<success_criteria>

- D-01..D-04 (save flow) honored: bulk save preserved, beforeunload preserved, dirty state drives top-bar button
- D-05..D-12 (nav/grouping) honored: progress + toggle present, JS-only grouping, no persistence, mata-mata por fase
- D-13..D-18 (cards) honored: linha única, header unificado, inputs 44px, knockout select preservado
- D-19..D-22 (feedback) honored: toast Django auto-dismiss, dirty laranja, fechado badge red, erro via Django messages
- All FR-01, FR-03, FR-09 verified by human checkpoint
  \</success_criteria>

<output>
After completion, create `.planning/02/02-03-SUMMARY.md` documenting:
- Final structure of `detail.html` (line counts before/after, sections refactored)
- Confirmation each D-XX decision was implemented (table mapping decision → location in template)
- Human checkpoint result and any deferred follow-ups (e.g., visual tweaks at 320px)
</output>
