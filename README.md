# PenniniBet

## Project Setup

1. Clone the repository:
   ```bash
   git clone
    ```

2. Create a local directory:
    ```bash
    mkdir local
    ```

3. Copy the development settings template:
    ```bash
    cp penninibet/src/settings/templates/settings.dev.py ./local/settings.dev.py
    ```
